// import React, { useContext } from "react";
// import { CookieContext } from "../App";
const Navigation = (props) => {
    // const d = useContext(CookieContext);
    console.log(props);
    return (
        <div className="container mt-5">This is Navigation of The website</div>
    );
};

export default Navigation;
